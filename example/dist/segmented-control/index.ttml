<view class="wux-class {{ classes.wrap }}" tt:if="{{ values.length }}">
    <block tt:for="{{ values }}" tt:key="index">
    	<view class="{{ classes.item }} {{ activeKey === index ? prefixCls + '__item--current' : '' }}" data-index="{{ index }}" bindtap="onTap">{{ item }}</view>
    </block>
</view>
