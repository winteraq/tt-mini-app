<view class="wux-class {{ classes.wrap }}" bindtap="onTap">
    <view class="{{ classes.title }}" tt:if="{{ title }}">{{ title }}</view>
    <block tt:else>
        <slot></slot>
    </block>
    <view class="{{ classes.bar }}" tt:if="{{ current }}"></view>
</view>
