<view class="wux-class {{ classes.wrap }}" style="{{ extStyle }}">
    <image src="{{ src }}" tt:if="{{ src }}" />
    <text class="{{ classes.string }}" style="{{ childrenStyle }}" tt:else>
        <slot></slot>
    </text>
</view>
