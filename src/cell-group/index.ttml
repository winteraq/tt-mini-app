<view class="wux-class {{ classes.wrap }}">
    <view class="{{ classes.hd }}" tt:if="{{ title }}">{{ title }}</view>
    <view class="{{ classes.bd }}">
        <slot></slot>
    </view>
    <view class="{{ classes.ft }}" tt:if="{{ label }}">{{ label }}</view>
</view>
